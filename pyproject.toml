[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "thorgor"
version = "0.1.0"
description = "Isolated Heroes of Newerth 3.2.7.1 LAN backend"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["thorgor*"]

