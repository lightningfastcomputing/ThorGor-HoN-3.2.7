ThorGor v72.1 CLEAN DIAGNOSTIC
2026-08-16

Correction from v72:
- v72 accidentally shipped with stale work/v31_registration_state.json from an earlier Test Game (match_id 13).
- START_V72_DIAGNOSTIC.bat cleared some protocol logs but did not clear all shared runtime state.
- That could make an old Test Game appear in the JOIN server list before a new lobby was created and contaminate diagnostics.

v72.1 changes:
- Package ships without the stale registration state/runtime logs.
- START_V72_DIAGNOSTIC.bat invokes RESET_V42.ps1 before startup.
- Also removes native-match bridge and UDP-shim volatile logs/state.
- K2 v65 and cgame v61 are unchanged.
- Master/chat diagnostic instrumentation is unchanged.

Expected clean-run behavior:
- Before hosting, there must be no pre-existing Test Game JOIN row.
- An idle CREATE vessel may still appear where HoN's Create Game flow expects an available server; that is distinct from a hosted game named Test Game.
