# Packet logger

The stable packet-evidence API is `thorgor.tools.packet_logger.PacketLogger`.
New captures should identify the protocol, direction, UTC timestamp, and raw
payload. Runtime captures remain ignored by Git until reviewed and redacted.

