# Authentication compatibility and endpoint redirects currently share the
# verified v57 binary generation; split the identities only when their writes
# can be independently composed.
PATCH_ID = "client.server_redirects_v57"
