# Protocol documentation

Protocol notes belong here by transport and command, with captured byte shape,
field interpretation, direction, client/server version, and a regression-test
reference. The stable implementations live in `thorgor/protocols`.

