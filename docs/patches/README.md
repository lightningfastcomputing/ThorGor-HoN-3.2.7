# Patch evidence

Each patch should have a focused note that expands its catalog manifest with
breakpoints, disassembly/decompiler locations, packet captures, reproduction
steps, and rollback observations. Runtime captures must be redacted before
being committed.

